<div class="card features">
    <div class="features-header">
        <div class="badge">
            <div class="tittle-badge">Почему мы?</div>
        </div>
        <h3 class="heading-style-h3">Наши преимущества</h3>
        <p class="paragraph-large">
            Опыт. Качество. Гарантия. — Надежность, проверенная временем
        </p>
    </div>

    <div class="faq-list">
        <div class="faq-accordion">
            <button class="faq-question" type="button">
                <span class="sub-heading-large">Более 14 лет на рынке</span>
                <span class="faq-icon">
                    <svg width="24" height="24" viewBox="0 0 12 12">
                        <path
                            fill="currentColor"
                            d="M6 2a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 6 2"
                        ></path>
                    </svg>
                </span>
            </button>

            <div class="faq-panel">
                <div class="faq-answer">
                    <p class="paragraph-regular">
                        Более 14 лет мы восстанавливаем работоспособность
                        бытовой техники — холодильников, стиральных машин,
                        газовых плит и другого оборудования. Заказывая услуги в
                        IronService, вы не рискуете ни деньгами, ни техникой. На
                        все работы предоставляется гарантия от 3 до 12 месяцев.
                    </p>
                </div>
            </div>
        </div>

        <div class="faq-accordion">
            <button class="faq-question" type="button">
                <span class="sub-heading-large">За что нас все любят?</span>
                <span class="faq-icon">
                    <svg width="24" height="24" viewBox="0 0 12 12">
                        <path
                            fill="currentColor"
                            d="M6 2a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 6 2"
                        ></path>
                    </svg>
                </span>
            </button>

            <div class="faq-panel">
                <div class="faq-answer">
                    <p class="paragraph-regular">
                        Ежегодно мы выполняем более 5000 ремонтных работ
                        различной сложности — от бытовых приборов до
                        компьютерной техники. Клиенты доверяют нам за
                        профессионализм, качество и оперативность.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card.features {
        display: grid;
        gap: 40px;
    }

    /* header */
    .features-header {
        display: flex;
        gap: 12px;
        align-items: flex-start;
        flex-direction: column;
    }

    /* faq list */
    .faq-list {
        display: flex;
        gap: 8px;
    }

    /* question */
    .faq-question {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 16px;
        background: none;
        border: none;
        cursor: pointer;
        text-align: left;
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    }

    .faq-question:hover {
        background-color: rgba(0, 0, 0, 0.02);
    }

    /* icon */
    .faq-icon svg {
        transition: transform 0.3s ease;
    }

    .faq-accordion[data-open="true"] .faq-icon svg {
        transform: rotate(45deg);
    }

    /* panel animation WITHOUT max-height */
    .faq-panel {
        display: grid;
        grid-template-rows: 0fr;
        transition: grid-template-rows 300ms ease;
    }

    .faq-panel > * {
        overflow: hidden;
    }

    .faq-accordion[data-open="true"] .faq-panel {
        grid-template-rows: 1fr;
    }

    /* answer */
    .faq-answer {
        padding: 12px 16px 20px;
        opacity: 0;
        transform: translateY(-6px);
        transition:
            opacity 200ms ease,
            transform 200ms ease;
    }

    .faq-accordion[data-open="true"] .faq-answer {
        opacity: 1;
        transform: translateY(0);
    }
</style>

<script is:inline>
    const accordions = document.querySelectorAll(".faq-accordion");

    accordions.forEach((accordion) => {
        const button = accordion.querySelector(".faq-question");

        button.addEventListener("click", () => {
            const isOpen = accordion.dataset.open === "true";

            accordions.forEach((a) => delete a.dataset.open);

            if (!isOpen) {
                accordion.dataset.open = "true";
            }
        });
    });
</script>
